# 附录B：剩余反编译残留点位表（2026-03-13，按当前代码现状回写）

**文档类型：** 当前执行附录  
**关联主文档：** `项目双端修复总计划.md`  
**关联规则文档：** `项目执行规则.md`  
**适用目的：** 只记录当前仍未完成的反编译点位。已修复完整的批次和已销账点位已从本表删除。  

---

## 1. 当前概览

| 类型 | 当前数量 | 说明 |
|---|---:|---|
| `switch (xxx.hashCode())` | `103` 处 | 分布在 `60` 个文件 |
| 占位/编号式命名残留 | `14` 个文件 | 包含 `XXX`、`CopyOf...`、`GemXXX`、`handleOperationN` |
| 可执行级硬残留 | `0` | 当前未扫到 `JD-Core`、`Method not decompiled`、`/* java... */`、可执行 `this$N/access$N` |

补充说明：

- `this$0` 当前只出现在 `TRACE[...]` 注释里，不计入剩余点位。
- `TODO Auto-generated ...` 当前不纳入本表，它属于历史噪音，不属于本轮反编译主点位。

---

## 2. 当前执行批次

| 批次 | 范围 | 状态 | 说明 |
|---|---|---|---|
| Batch-R01 | 服务端后台查询/排序链 | 未完成 | 先清 servlet/service 中剩余的 `hashCode` 分派 |
| Batch-R02 | 服务端战斗/计算链 | 未完成 | 当前最大热点，文件多且链路深 |
| Batch-R03 | 客户端翅膀/藏宝阁热点 | 未完成 | 客户端面板、筛选、品质映射仍保留大量 `hashCode` 分派 |
| Batch-R04 | 客户端通用 UI/控制链 | 未完成 | 剩余工具类和控制类仍有结构性残留 |
| Batch-R05 | 套装/翅膀服务端编号入口 | 未完成 | `handleOperationN` 仍是当前主要占位命名来源 |
| Batch-R06 | 双端占位类名/复制类名 | 未完成 | `XLPath.XXX`、`CopyOf...`、`GemXXX` 仍未收口 |

---

## 3. 当前未完成点位

| ID | 范围 | 代表文件 | 当前特征 | 状态 | 处理建议 |
|---|---|---|---|---|---|
| R-01 | 服务端后台查询/排序链 | `Gameserver/src/main/java/org/come/servlet/PackControlServerlrt.java`、`RoleSummoningRoleUserServlet.java`、`MountRoleUserServlet.java`、`LingBaoRoleUserServlet.java`、`org/come/serviceImpl/UserTableServiceImpl.java` | `switch (xxx.hashCode())` | 未完成 | 先恢复为普通字符串分支，再保留原排序/状态语义 |
| R-02 | 服务端协议/控制分派补漏 | `Gameserver/src/main/java/org/come/handler/MainServerHandler.java`、`org/come/tool/TalentTool.java`、`org/come/readUtil/ReadGolemConfig.java`、`org/come/action/lottery/HDXRoleAction.java`、`org/come/bean/LoginResult.java` | `switch (xxx.hashCode())` | 未完成 | 按调用链收口，不再新增旧式分派 |
| R-03 | 服务端战斗/计算链 | `Gameserver/src/main/java/come/tool/Calculation/CalculationUtil.java`、`come/tool/FightingData/Ql.java`、`Battlefield.java`、`ManData.java`、`MixDeal.java`、`UsePetAction.java`、`UseLingAction.java`、`HurtAction.java` | 高频 `hashCode` 分派 | 未完成 | 作为单独批次推进，避免和 UI/后台链混做 |
| R-04 | 客户端翅膀/藏宝阁热点 | `GameClient/src/org/wing/panel/WingMainPanel.java`、`LHMainPanel.java`、`org/wing/mouseListener/WingUpStarMouseListener.java`、`LHUpStarMouseListener.java`、`org/cbg/until/TreasurePavilionSearchUntil.java`、藏宝阁相关面板 | 高频 `hashCode` 分派 | 未完成 | 先按模块集中清理，再统一校正提示和展示逻辑 |
| R-05 | 客户端通用 UI/控制链 | `GameClient/src/org/come/until/Util.java`、`CutButtonImage.java`、`ControlNpcXmlUntil.java`、`AccessSuitMsgUntil.java`、`AccessNedanMsgUntil.java`、`AssetControl.java`、`NPCJpanel.java`、`MsgJapnel.java`、`ShopPriceControl.java` 等 | `hashCode` 分派 | 未完成 | 按共享工具批次收口，避免零散补点 |
| R-06 | 套装/翅膀服务端编号入口 | `Gameserver/src/main/java/org/come/action/suit/SuitComposeAction.java`、`WingCompose.java`、`StarCard.java`、`NpcCompose.java`、`GemCompose.java`、`GemIntensify.java`、`SuitPetEquip.java`、`SuitPalEquip.java` | `handleOperationN` | 未完成 | 改为语义化入口，保留兼容层但不再把编号入口作为主入口 |
| R-07 | 双端占位类名/复制类名 | `GameClient/src/come/tool/map/XLPath.java`、`Gameserver/src/main/java/org/come/script/XLPath.java`、`GameClient/src/org/skill/panel/CopyOfSkillTYCPanel.java`、`GameClient/src/org/come/action/CopyOfSkillTYCMuse.java`、`GameClient/src/com/tool/btn/GemMakeBtn.java` | `class XXX`、`recordXXX()`、`CopyOf...`、`GemXXX()` | 未完成 | 双端同步重命名并补行为注释 |

---

## 4. 当前判断

1. 附录B不再保留“已完成批次”与“已销账点位”。
2. 当前反编译主线仍未完成，后续执行只围绕 `R-01` ~ `R-07` 展开。
3. 每完成一批，必须同步回写本表和 `项目双端修复总计划.md`，否则不视为完成。
