# 玄宝端游戏项目

这是一个包含游戏客户端与服务端的 Java 项目。

## 项目结构

- `GameClient/`：游戏客户端
- `Gameserver/`：游戏服务器（Java Web + Netty）
- `数据库/`：Oracle 初始化与还原脚本

## 当前文档入口

- `项目双端修复总计划.md`：当前唯一权威修复计划与状态文档
- `项目执行规则.md`：执行前检查、修复顺序、验证与回写规则
- `当前项目反编译残留审查报告-2026-03-12.md`：当前代码现状复核结论
- `附录B-剩余反编译残留点位表-2026-03-13.md`：当前未完成反编译点位清单

## 当前反编译状态

- 当前**不能**判定为“反编译残留清零”。
- 已确认没有可执行级的 `JD-Core`、`Method not decompiled`、`/* java... */`、`access$N` 这类硬残留。
- 当前仍有**结构性反编译残留**：
  - `switch (xxx.hashCode())`：`103` 处，分布在 `60` 个文件
  - 占位/编号式命名残留：`14` 个文件
- `TODO Auto-generated ...` 这类历史噪音仍有保留，但当前不计入本轮反编译主点位统计。

## 说明

- 根目录中的反编译相关文档已按当前代码现状回写，已完成的反编译批次和销账点位不再保留为执行项。
- `项目执行规则.md` 中的硬性规则保持不变，后续修复仍按“先复扫、再补录、再修复、再回写”的闭环执行。
