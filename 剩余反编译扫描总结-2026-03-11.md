# 剩余反编译扫描总结（2026-03-11 文件名保留，2026-03-13 按当前代码现状回写）

**扫描范围：** `GameClient/src/`、`Gameserver/src/main/java/`  
**统计口径：** 仅统计当前代码中仍能直接定位的反编译结构残留；已完成销账的批次和历史“强嫌疑/中嫌疑”记录不再保留为当前执行项。  
**关联文档：** `项目双端修复总计划.md`、`当前项目反编译残留审查报告-2026-03-12.md`、`附录B-剩余反编译残留点位表-2026-03-13.md`

---

## 1. 当前汇总

| 类型 | 当前数量 | 说明 |
|---|---:|---|
| `switch (xxx.hashCode())` | `103` 处 | 分布在 `60` 个文件 |
| 占位/编号式命名残留 | `14` 个文件 | 包含 `XXX`、`CopyOf...`、`GemXXX`、`handleOperationN` |
| 可执行级硬残留 | `0` | 未扫到 `JD-Core`、`Method not decompiled`、`/* java... */`、可执行 `this$N/access$N` |

补充说明：

- `this$0` 关键词在当前仓库里仍能搜到 `6` 次，但都只出现在 `TRACE[...]` 注释中，不是可执行残留。
- `TODO Auto-generated ...` 仍有 `140` 处，这些属于历史噪音，当前不计入本轮反编译主点位表。

---

## 2. 未完成热点 Top 10

下表按 `switch (xxx.hashCode())` 出现次数排序，仅反映当前剩余热点，不包含已完成批次。

| 文件 | 当前次数 | 备注 |
|---|---:|---|
| `GameClient/src/org/wing/panel/WingMainPanel.java` | `8` | 客户端翅膀主面板，仍有多段字符串分派 |
| `GameClient/src/org/wing/panel/LHMainPanel.java` | `7` | 客户端良号翅膀主面板，同类问题集中 |
| `Gameserver/src/main/java/come/tool/Calculation/CalculationUtil.java` | `7` | 服务端战斗计算链热点 |
| `Gameserver/src/main/java/come/tool/FightingData/Ql.java` | `6` | 服务端战斗状态/效果链热点 |
| `GameClient/src/org/cbg/until/TreasurePavilionSearchUntil.java` | `5` | 客户端藏宝阁筛选/检索分派 |
| `Gameserver/src/main/java/come/tool/FightingData/Battlefield.java` | `3` | 服务端战场主链 |
| `Gameserver/src/main/java/come/tool/FightingData/ManData.java` | `3` | 服务端战斗角色数据链 |
| `Gameserver/src/main/java/org/come/servlet/PackControlServerlrt.java` | `2` | 服务端后台查询 servlet |
| `Gameserver/src/main/java/org/come/action/suit/SuitComposeAction.java` | `2` | 套装总分发链仍夹杂字符串分派 |
| `GameClient/src/org/come/until/AccessSuitMsgUntil.java` | `2` | 客户端套装消息整理链 |

---

## 3. 占位/编号式命名热点

下表列出当前仍明显带有反编译语义的文件，不再保留已完成销账项。

| 文件 | 当前特征 | 说明 |
|---|---|---|
| `Gameserver/src/main/java/org/come/action/suit/SuitComposeAction.java` | `handleOperation0/120/127/123/124` | 总分发入口仍以编号方法组织 |
| `Gameserver/src/main/java/org/come/action/suit/NpcCompose.java` | `handleOperation71` ~ `handleOperation81` | Npc 合成链仍保留编号入口 |
| `Gameserver/src/main/java/org/come/action/suit/StarCard.java` | `handleOperation51` ~ `handleOperation59` | 星卡链仍保留编号入口 |
| `Gameserver/src/main/java/org/come/action/suit/WingCompose.java` | `handleOperation31` 等旧编号入口 | 虽已有语义化调用，但编号入口仍在 |
| `Gameserver/src/main/java/org/come/action/suit/SuitPetEquip.java` | `handleOperation41` ~ `46` | 宠装链仍为编号入口 |
| `Gameserver/src/main/java/org/come/action/suit/GemCompose.java` | `handleOperation17` ~ `21` | 宝石合成仍为编号入口 |
| `Gameserver/src/main/java/org/come/action/suit/GemIntensify.java` | `handleOperation101` ~ `104` | 宝石强化仍为编号入口 |
| `GameClient/src/come/tool/map/XLPath.java` | `class XXX`、`recordXXX()` | 客户端寻路核心仍有占位类名 |
| `Gameserver/src/main/java/org/come/script/XLPath.java` | `class XXX`、`recordXXX()` | 服务端寻路核心仍有占位类名 |
| `GameClient/src/org/skill/panel/CopyOfSkillTYCPanel.java` | `CopyOf...` | 历史复制类名仍在主链使用 |
| `GameClient/src/org/come/action/CopyOfSkillTYCMuse.java` | `CopyOf...` | 客户端技能动效监听仍为复制类名 |
| `GameClient/src/com/tool/btn/GemMakeBtn.java` | `GemXXX()` | 客户端宝石逻辑仍保留占位方法名 |

---

## 4. 当前判断

- 前几轮修复**清掉了明确挂账的硬残留和批量注释残留**，但没有覆盖完整仓库范围。
- 当前主问题已经从“会不会编不过”转成“结构性反编译残留没有收干净”。
- 下一轮不应继续按零散会话补点，而应直接按附录B的剩余批次逐文件销账。

---

## 5. 当前建议顺序

1. 先清服务端后台查询/排序类 `hashCode` 分派：`PackControlServerlrt`、`RoleSummoningRoleUserServlet`、`MountRoleUserServlet`、`LingBaoRoleUserServlet`、`UserTableServiceImpl`。
2. 再清服务端战斗/计算主链：`CalculationUtil`、`Ql`、`Battlefield`、`ManData`、`MixDeal`、`UsePetAction`。
3. 再清客户端翅膀与藏宝阁热点：`WingMainPanel`、`LHMainPanel`、`TreasurePavilionSearchUntil`、相关按钮/筛选面板。
4. 最后处理编号式入口和占位命名：`SuitComposeAction` 链、双端 `XLPath`、`CopyOfSkillTYCPanel`、`GemXXX`。
