# 附录A：双端反编译点对点扫描结果（2026-03-11）

**文档类型：** 补充扫描说明  
**关联主文档：** `项目双端修复总计划.md`  
**扫描范围：** `GameClient/src/`、`Gameserver/src/main/java/`  
**扫描目的：** 识别双端仍然残留的反编译痕迹、区分业务拼音命名与反编译命名，并建立后续点对点修复清单。

---

## 1. 扫描方法

本次扫描没有把“拼音命名”直接等同于“反编译痕迹”，而是采用如下识别规则：

### 1.1 视为反编译痕迹的高优先级特征
- 参数名或局部变量名为 `p0`、`p1`、`var1`、`var2` 等占位符。
- 方法名为 `type31`、`type355` 这类失去业务语义的编号型命名。
- 方法名出现 `getpart`、`getpartAll`、`ChangePart`、`ChangeTalent` 这类大小写与语义混杂的命名。
- 存在明显反编译注释头，例如 `Source code recreated from a .class file...`。

### 1.2 视为业务拼音命名、暂不作为反编译问题处理的特征
- 明显来自业务语义的拼音字段，例如：
  - `qizhi`
  - `neili`
  - `zhili`
  - `naili`
  - `mingqi`
  - `daode`
  - `panni`
  - `wanxing`
  - `qingmi`
  - `xiaoxin`
  - `wenbao`
  - `yangyujin`
- 明显属于业务系统名称或约定俗成的拼音词，例如：
  - `lingbao`
  - `lianghao`
  - `lianhua`
  - `tianfu`
  - `fushi`

### 1.3 点对点扫描原则
- 如果服务端某个模块出现明显反编译痕迹，则继续检查客户端是否存在同主题或同名模块。
- 优先修“服务端 + 客户端”都存在痕迹的模块。
- 对只有服务端存在的反编译问题，也会记录为“服务端主导模块”，后续再通过协议与调用链映射到客户端。

---

## 2. 全量扫描结果概览

### 2.1 文件规模
- 服务端 Java 文件：`1439`
- 客户端 Java 文件：`1858`

### 2.2 反编译特征计数
- 服务端 `pN` 占位参数/变量：`1434`
- 客户端 `pN` 占位参数/变量：`230`
- 服务端 `varN` 占位变量：`83`
- 客户端 `varN` 占位变量：`231`
- 服务端 `typeN(...)` 方法：`115`
- 客户端 `typeN(...)` 方法：`90`
- 客户端残留 FernFlower 反编译头：`5`
- 服务端残留 FernFlower 反编译头：`0`

### 2.3 结论
- **服务端的反编译占位参数问题明显重于客户端。**
- **客户端的 `varN` 局部变量与少量反编译头残留更突出。**
- **适合继续采用“服务端主导 + 客户端对位”的点对点修复策略。**

---

## 3. 当前已完成首轮修复的双端模块

### 3.1 Wing 模块

#### 服务端
- `Gameserver/src/main/java/org/come/action/suit/WingCompose.java`
- `Gameserver/src/main/java/org/come/action/suit/SuitComposeAction.java`

#### 客户端
- `GameClient/src/org/wing/panel/WingMainPanel.java`

#### 当前状态
- 已完成**首轮语义修复**。
- 已将服务端编号型入口恢复为语义化方法名，并保留兼容入口。
- 已为客户端页签编号补充常量语义说明。

#### 后续仍需继续的点
- 协议字段与资源对应关系。
- 材料消耗、产出与客户端提示的完全对齐。
- 相关类的注释覆盖率继续提升。

### 3.2 Baby 模块

#### 服务端
- `Gameserver/src/main/java/org/come/entity/Baby.java`
- `Gameserver/src/main/java/org/come/service/IBabyService.java`
- `Gameserver/src/main/java/org/come/mapper/BabyMapper.java`
- `Gameserver/src/main/java/come/tool/Good/UseBabyAction.java`

#### 客户端
- `GameClient/src/org/come/entity/Baby.java`
- `GameClient/src/org/come/good/BabyGood.java`
- `GameClient/src/org/come/Jpanel/TestChildJpanel.java`
- `GameClient/src/org/come/mouslisten/BabyPartsMouslisten.java`
- `GameClient/src/com/tool/btn/BabyBtn.java`

#### 当前状态
- 已完成**首轮语义修复**。
- 已将 `getpart/getpartAll/ChangePart/ChangeTalent` 恢复为语义化方法名，并保留兼容入口。
- 已同步修正一组双端核心调用点。

#### 后续仍需继续的点
- 更多客户端孩子面板调用链。
- Mapper 与 XML 的深层对齐。
- 更高覆盖率的行为注释与业务注释。

---

## 4. 双端点对点重点疑似模块清单

以下模块在双端中都发现了明显的反编译特征，建议按优先级继续推进。

### 4.1 A 级：`Skill`

#### 服务端
- `Gameserver/src/main/java/org/come/model/Skill.java`

#### 客户端
- `GameClient/src/org/come/bean/Skill.java`

#### 证据
- 服务端可疑分值：`35`
- 客户端可疑分值：`35`

#### 判定
- 这是当前**双端同名模块中最值得继续做点对点修复**的一组。

#### 建议动作
- 先对比两端字段与 getter/setter 语义。
- 再检查依赖该类的协议、技能面板、战斗逻辑与服务端技能模型。

### 4.2 A 级：`Scene`

#### 服务端
- `Gameserver/src/main/java/come/tool/Scene/Scene.java`

#### 客户端
- `GameClient/src/come/tool/Scene/Scene.java`

#### 证据
- 服务端可疑分值：`17`
- 客户端可疑分值：`5`

#### 判定
- 属于双端公共基础类，虽然客户端痕迹轻于服务端，但仍适合做对位修复。

#### 建议动作
- 先确认该类是否为地图/场景公共载体。
- 再检查是否存在字段顺序、状态位或方法语义不一致。

### 4.3 B 级：`RoleSummoning`

#### 服务端
- `Gameserver/src/main/java/org/come/entity/RoleSummoning.java`
- `Gameserver/src/main/java/org/come/service/IRoleSummoningService.java`

#### 客户端
- `GameClient/src/org/come/entity/RoleSummoning.java`

#### 判定
- 虽然同名文件直接分值不高，但它是双端实体，对业务影响大。
- 适合在 `Baby` 后作为下一个实体型模块继续做点对点修复。

### 4.4 B 级：`AES/协议加解密`

#### 当前状态
- 这组已完成安全收口与首轮统一：
  - 客户端主链路 AES
  - 服务端主链路 AES
  - 服务端主动推送 AES 与客户端解密链路

#### 后续建议
- 后面继续把常量逐步收敛到统一配置读取入口。
- 检查是否还有未统一的协议分支使用旧工具类。

---

## 5. 服务端主导、需继续映射客户端的重点模块

以下模块在服务端反编译痕迹很重，但客户端未必存在同名文件，需要通过协议或业务主题继续映射。

### 5.1 `SuitComposeAction`
- 路径：`Gameserver/src/main/java/org/come/action/suit/SuitComposeAction.java`
- 可疑分值：`189`
- 判定：当前是服务端最重的业务分发类之一。
- 客户端对应方向：套装、翅膀、宝石、星卡、炼化等界面与按钮事件。

### 5.2 `RoleTableMapper` / `UsertableMapper` / `IUserTableService` / `IRoleTableService`
- 判定：这组集中体现了服务端数据层的反编译参数问题。
- 客户端对应方向：登录、角色列表、账号管理相关协议。

### 5.3 `GoodstableMapper` / `IGoodsTableService`
- 判定：商品、背包、仓库、交易相关的主数据链路。
- 客户端对应方向：背包、商店、摆摊、装备与物品面板。

---

## 6. 建议的点对点修复顺序

### 第一优先级
1. `Skill` 双端点对点修复
2. `Scene` 双端点对点修复
3. `RoleSummoning` 双端实体链路修复

### 第二优先级
4. `RoleTable/Usertable` 服务端主链路修复，并映射客户端登录/角色协议
5. `Goodstable` 服务端主链路修复，并映射客户端背包/物品面板

### 第三优先级
6. `SuitComposeAction` 继续按功能块拆分，逐个对位客户端功能面板

---

## 7. 当前扫描结论

### 7.1 识别原则结论
- 该项目里**拼音命名很多，但不等于反编译**。
- 真正应优先修的是：
  - 占位参数名
  - 编号型方法名
  - 大小写异常方法名
  - 双端不一致的实体/协议/配置/Mapper

### 7.2 执行策略结论
- 后续应继续采用“**服务端发现问题 → 客户端查对应模块 → 双端同步修**”的方式推进。
- 每修一个点，都要检查：
  - 服务端逻辑
  - 客户端展示/调用
  - 协议/字段
  - Mapper/配置
  - 注释覆盖率

### 7.3 当前最值得继续推进的点对点模块
- `Skill`
- `Scene`
- `RoleSummoning`

---

## 8. 本附录的作用边界

- 本文件是**扫描附录**，不是新的总计划。
- 后续执行仍以 `项目双端修复总计划.md` 为准。
- 本文件只用于记录扫描证据、模块对位关系和后续修复入口。
