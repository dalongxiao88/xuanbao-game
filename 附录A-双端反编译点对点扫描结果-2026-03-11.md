# 附录A：双端未完成反编译热点对位表（2026-03-11 文件名保留，2026-03-13 按当前代码现状回写）

**文档类型：** 当前热点对位附录  
**关联主文档：** `项目双端修复总计划.md`  
**适用目的：** 只保留当前仍未完成的双端反编译热点映射关系；已完成的历史模块和旧批次不再保留为本附录内容。  

---

## 1. 使用说明

- 本附录不再保留 `2026-03-11` 的“强嫌疑/中嫌疑”全量历史统计。
- 本附录只回答两个问题：
  1. 当前剩余反编译热点在哪些链路上。
  2. 服务端和客户端应该如何对位推进。

---

## 2. 当前双端热点对位

| 热点链路 | 服务端文件 | 客户端文件 | 当前残留类型 | 当前状态 | 建议顺序 |
|---|---|---|---|---|---|
| 套装/翅膀/星卡/宝石链 | `SuitComposeAction`、`WingCompose`、`StarCard`、`NpcCompose`、`GemCompose`、`GemIntensify`、`SuitPetEquip`、`SuitPalEquip` | `WingMainPanel`、`LHMainPanel`、`WingUpStarMouseListener`、`LHUpStarMouseListener`、`GemMakeBtn`、`CopyOfSkillTYCPanel` | `handleOperationN`、`hashCode` 分派、占位命名 | 未完成 | 先服务端入口语义化，再对位客户端面板和按钮链 |
| 后台查询/排序链 | `PackControlServerlrt`、`RoleSummoningRoleUserServlet`、`MountRoleUserServlet`、`LingBaoRoleUserServlet`、`UserTableServiceImpl`、`MainServerHandler` | `TreasurePavilionSearchUntil`、藏宝阁相关面板、`TimeLimit`、`RoleSummoning` | `hashCode` 分派 | 未完成 | 先统一服务端排序/状态分派，再回收客户端筛选映射 |
| 寻路/脚本链 | `org/come/script/XLPath` | `come/tool/map/XLPath` | `class XXX`、`recordXXX()` | 未完成 | 双端同时重命名并补行为注释 |
| 战斗/计算主链 | `CalculationUtil`、`Ql`、`Battlefield`、`ManData`、`MixDeal`、`UsePetAction`、`UseLingAction`、`HurtAction` | `EffectType`、技能/状态相关按钮与消息整理类 | `hashCode` 分派、局部结构性残留 | 未完成 | 先服务端集中清理，再补客户端对位复核 |
| 通用客户端 UI/控制链 | — | `Util`、`CutButtonImage`、`ControlNpcXmlUntil`、`AccessSuitMsgUntil`、`AccessNedanMsgUntil`、`AssetControl`、`NPCJpanel`、`MsgJapnel`、`ShopPriceControl` | `hashCode` 分派 | 未完成 | 按共享工具批次收口 |

---

## 3. 当前判断

- 当前最值得继续做“端对端同步修复”的，是**套装/翅膀链**和**双端 XLPath**。
- 当前最值得先做“服务端主导清理”的，是**后台查询/排序链**和**战斗/计算链**。
- 当前客户端剩余残留虽然分布更散，但 `Wing`、`LH`、`藏宝阁` 三组热点仍然明显，不能再按“客户端已基本清空”处理。

---

## 4. 本附录的作用边界

- 本文件只保留当前未完成热点，不再回放已完成模块。
- 执行时仍以 `项目双端修复总计划.md` 和 `附录B-剩余反编译残留点位表-2026-03-13.md` 为准。
