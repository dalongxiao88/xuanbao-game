# 项目执行规则
**创建日期：** 2026-03-12  
**适用范围：** `GameClient/`、`Gameserver/`、`数据库/`  
**文档定位：** 本文档是基于 `项目双端修复总计划.md` 提炼出的执行约束文档，用于规范每次任务开始、执行、验证与回写的统一动作。本文档不替代主文档；涉及阶段目标、完成状态与历史记录时，仍以 `项目双端修复总计划.md` 为准。

---

## 1. 文档优先级

1. `项目双端修复总计划.md` 是项目修复计划与状态记录的主文档。
2. `项目执行规则.md` 是执行动作约束文档，负责约束“开始前做什么、修复时怎么做、完成后怎么回写”。
3. 若两份文档出现冲突，优先服从主文档；若本规则文档更严格，则按更严格要求执行。

---

## 2. 开工前强制动作

1. 每次开始任何修复、排查、补注释、改协议、改 Mapper、改配置前，必须先通读 `项目双端修复总计划.md` 与 `项目执行规则.md`。
2. 未完成上述阅读，不得开始当次任务。
3. 开工前必须先确认本次任务属于哪个模块、影响哪一端、是否涉及协议、Mapper、XML、配置或资源。
4. 开工前必须先确认主文档里的最近状态记录，避免重复修复、跳过未完成链路或沿用过期结论。

---

## 3. 执行总原则

1. 代码真相优先，任何判断必须服从代码现状。
2. 安全优先于重构，先去高危逻辑，再谈结构优化。
3. 结构冻结优先，非必要不做大范围目录、类结构和调用层级重排。
4. 端对端闭环优先，不允许只修服务端或只修客户端就判定完成。
5. 注释与修复同步，不允许代码改完后再补注释。
6. 不猜业务语义，无法完全确认时先写行为性注释，并在主文档中保留待确认状态。

---

## 4. 单次任务执行顺序

每次修一个模块或一条链路，必须按以下顺序推进：

1. 确认服务端入口与调用链。
2. 确认客户端入口与对应 UI、事件、展示链。
3. 确认协议号、字段含义、常量、参数结构。
4. 确认数据库实体、Mapper、XML、初始化脚本影响面。
5. 完成双端代码修复。
6. 为修复范围补齐注释，达到主文档要求。
7. 做模块级或链路级验证。
8. 更新 `项目双端修复总计划.md`，记录实际完成项、剩余项、风险项或阻塞项。

---

## 5. 硬性规则

1. 每修复一处，必须同步更新 `项目双端修复总计划.md`。
2. 未更新主文档的修复，一律视为“未完成”，不得口头或文档上宣告完成。
3. 每次执行任务前，必须先读 `项目双端修复总计划.md` 与 `项目执行规则.md`，读完后才能开始工作。
4. 任一修复如果影响双端链路、协议、Mapper、配置、SQL 或资源映射，主文档记录中必须明确写出影响范围。

---

## 6. 注释要求

1. 修复涉及文件的结构化注释覆盖率不得低于 `75%`。
2. 新建文件或重度修复文件的注释覆盖率目标为 `85%` 以上。
3. 类、接口、公开方法、复杂私有方法、关键字段、常量、关键 SQL 块、协议分支、危险逻辑分支必须补注释。
4. 注释要说明“做什么、为什么、影响什么”，不能写空泛描述。
5. 无法确认完整业务语义时，允许先写行为性注释，但不能伪造业务结论。
6. **每个修复点必须带可追溯注释。**
7. 可追溯注释至少要包含：
   - 点位表 ID，例如 `S-01`、`C-01`
   - 修复日期
   - 修复目的或收口原因
8. 可追溯注释推荐格式：
   - `TRACE[S-01][2026-03-13]: 恢复被反编译破坏的路径拼接语义。`
9. 若一次提交同时修多个点位，不允许只在文档中记录；每个点位所在代码附近都必须能找到对应的可追溯注释。

---

## 7. 完成判定

某个模块或链路只有同时满足以下条件，才可以在主文档中标记为“已完成”：

1. 影响范围已明确。
2. 双端代码已同步修复。
3. 数据库、Mapper、配置已同步核对并修复。
4. 高危逻辑已移除或替换为安全实现。
5. 注释覆盖率达到要求。
6. 主文档状态已同步更新。

只满足部分条件时，只能标记为“进行中”。

---

## 8. 文档回写规则

1. 主文档只记录实际完成项、剩余项、阻塞项与验证结论。
2. 不允许把推测、希望、计划中的结果写成“已完成”。
3. 若本次任务只是局部收口，也必须在主文档中明确写成“首轮”“第二轮”或“继续收口”，不能夸大范围。
4. 若发现旧统计与当前代码现状不一致，应先在主文档中注明口径差异，再继续推进修复。

---

## 9. 执行检查清单

每次任务开始前与结束后，至少完成以下检查：

### 开始前
- 已阅读 `项目双端修复总计划.md`。
- 已阅读 `项目执行规则.md`。
- 已确认本次任务模块与影响范围。
- 已确认是否涉及双端、协议、Mapper、配置、资源。

### 完成后
- 已完成代码修改。
- 已完成必要注释补充。
- 已完成基本验证或说明未验证原因。
- 已回写 `项目双端修复总计划.md`。

---

## 10. 长期约束

1. 后续所有修复任务都必须同时遵守主文档与本规则文档。
2. 若执行方式发生变化，应优先更新主文档，再更新本规则文档。
3. 未经更新的旧习惯、旧口头约定、旧临时说明，不得覆盖这两份文档。
