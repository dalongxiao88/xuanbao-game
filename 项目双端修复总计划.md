# 项目双端修复总计划

**创建日期：** 2026-03-10  
**适用范围：** `GameClient/`、`Gameserver/`、`数据库/`  
**文档定位：** 本文档为当前仓库唯一权威修复计划，后续修复、验收、注释补充与阶段推进均以本文档为准。

---

## 1. 文档治理

### 1.1 保留文档
- `README.md`：仓库入口说明。
- `项目双端修复总计划.md`：唯一权威修复计划。

### 1.2 清理文档
以下文档已不再作为执行依据，应从仓库根目录清理，原因是存在**状态失真、互相矛盾、阶段结论未经代码验证**等问题：

- `Master_Restoration_Plan.md`
- `Security_Analysis_Report.md`
- `Stage1修复跟踪文档.md`
- `Stage1收尾任务文档.md`
- `Stage1收尾执行进度报告.md`
- `下一步修复计划.md`
- `二次验证报告.md`
- `修复工作验证报告.md`
- `修复进度报告.md`
- `反编译代码分析报告.md`
- `文档对比分析与补充建议.md`
- `新建文本文档.txt`

### 1.3 文档使用规则
- 以后新增的阶段性记录不得替代本计划文档。
- 若需要补充分析，只能作为附录或补充说明，且必须明确标注日期、范围与验证结论。
- 任何“已完成”结论必须以代码现状、双端链路和可运行性验证为依据，不能仅根据推断或局部修改宣布完成。

---

## 2. 修复总目标

本项目当前处于**反编译代码清理与高危逻辑排雷阶段**，总目标不是立即重构，而是先建立一套可持续修复的稳定基线。

### 2.1 阶段性总目标
1. **先清恶意代码与危险逻辑**，确保项目具备继续修复的安全前提。
2. **先保持原始结构不变**，避免在反编译项目中因结构重排导致调用链失真。
3. **按模块做双端同步修复**，保证服务端、客户端、协议、配置、数据库映射始终一致。
4. **补足缺失注释**，使后续维护者能基于代码真实行为继续推进迭代。
5. **在基线稳定后再做迭代优化**，包括命名优化、冗余清理、模块拆分与结构改善。

---

## 3. 两个核心原则

### 3.1 核心一：端对端同时修复

任何模块修复必须采用“端对端同步”策略，禁止只改一端就宣告完成。

### 模块完成的最低条件
某个模块只有在以下链路全部对齐后，才可以从“修复中”变为“已完成”：

- **服务端逻辑已修复**：核心业务代码、动作类、服务类、实体类已校正。
- **客户端逻辑已修复**：界面、交互、协议触发、展示逻辑与服务端一致。
- **协议/常量已对齐**：协议号、字段含义、加密方式、参数结构没有双端漂移。
- **数据库与映射已对齐**：实体、Mapper、XML、初始化脚本与业务字段一致。
- **配置与资源已对齐**：密钥、地址、版本、资源文件、更新机制不再互相冲突。
- **注释与文档已同步**：修复涉及的核心类、方法、字段和 SQL 已补充说明。

### 端对端修复的执行顺序
每次修一个模块，统一按下面的顺序推进：

1. 确认服务端入口与调用链。
2. 确认客户端入口与对应 UI/事件链。
3. 确认协议与字段映射。
4. 确认数据库实体、Mapper、脚本影响面。
5. 完成双端代码修复。
6. 为修复范围补充注释。
7. 做模块级验证并更新计划状态。

### 3.2 核心二：注释覆盖率达到 75% 以上

### 注释目标
- 所有**新修复代码**必须同步补充注释。
- 所有**修复涉及到的老代码文件**，若注释明显缺失，必须在本次修复中补齐到可维护状态。
- 注释必须基于代码**实际行为**与**可验证逻辑**编写，不允许凭空猜测业务语义。

### 注释覆盖率统计口径
为避免把注释工作做成“逐行翻译”，本项目采用**结构化注释覆盖率**统计，而非简单的行数占比。

#### 计入统计的结构化单元
- 类、接口、枚举。
- 公开方法。
- 复杂私有方法（满足任一条件：分支较多、涉及协议/SQL、涉及外部资源、长度较长）。
- 关键字段、常量、配置项。
- 关键 SQL 块、协议号分支、危险逻辑分支。

#### 覆盖率要求
- **修复涉及文件：注释覆盖率不得低于 75%。**
- **新建文件或重度修复文件：注释覆盖率目标为 85% 以上。**
- 对暂时无法完全还原语义的反编译逻辑，允许先写“行为性注释”，明确其输入、输出、依赖与副作用。

### 注释编写规则
- 说明“做什么、为什么、影响什么”，不写空泛注释。
- 能确认业务语义时，写业务语义注释。
- 语义不完全确定时，写行为性注释，不强行猜名称。
- MyBatis XML、批量 SQL、协议分支、加密逻辑、危险执行路径必须写注释。

---

## 4. 当前已确认的高优先级问题

以下问题已通过代码现状确认，属于必须优先处理的范围：

### 4.1 客户端危险执行链路
- `GameClient/src/org/come/log/LanderJPanel.java`：执行 `.dat` 文件。
- `GameClient/src/org/come/log/UpdateMouslisten.java`：执行 `.dll` 文件。
- `GameClient/src/org/updateNew/UpdateNewFile.java`：下载并启动 `sdls.dll`、`Multiple.exe`、`gatU.bat`。

### 4.2 服务端高危限制与追踪逻辑
- `Gameserver/src/main/java/org/come/until/AllServiceUtil.java`：存在时间炸弹逻辑。
- `Gameserver/src/main/java/org/come/server/GameServer.java`：存在授权时间门槛与硬件 ID 获取逻辑。

### 4.3 双端加密与配置不一致
- 服务端与客户端 AES 密钥仍存在不一致与硬编码问题。
- 数据库地址、账号、密码等敏感配置仍直接保存在仓库内。

### 4.4 Mapper 与数据库初始化存在显性错误
- 多个 MyBatis XML 存在明显拼写/批量 SQL 问题。
- Oracle 初始化脚本仍是手工执行风格，缺少稳定的最小可运行说明。

---

## 5. 修复阶段规划

### 5.1 第零阶段：建立可信基线

**目标：** 停止继续被旧文档误导，建立“以代码为准”的执行基线。

### 输出物
- 清理历史误导性文档。
- 建立本计划文档。
- 明确当前高危文件与优先级。
- 明确注释覆盖率统计口径。

### 5.2 第一阶段：安全修复与结构冻结

**目标：** 在不调整原始项目结构的前提下，移除恶意代码、危险执行链路和明显的授权/追踪逻辑。

### 范围
- 客户端危险外部执行。
- 服务端时间炸弹、授权门槛、硬件追踪。
- 双端加密硬编码。
- 仓库内敏感配置脱敏。

### 约束
- 不做大规模重命名。
- 不迁移包结构。
- 不进行非必要 UI 重绘与功能改版。

### 5.3 第二阶段：最小可运行链路修复

**目标：** 在保留结构的前提下，把项目修到“至少能稳定验证关键链路”。

### 范围
- 修复显性 Mapper/XML 语法问题。
- 补齐最小配置样例。
- 梳理 Oracle 与 Redis 启动依赖。
- 确认服务端启动链路和客户端基础连接链路。

### 5.4 第三阶段：翅膀系统端对端同步修复

**目标：** 以 `Wing` 模块为样板，完整验证“服务端 + 客户端 + 协议 + 数据 + 注释”同步修复流程。

### 范围
- `WingCompose` 服务端业务语义还原。
- `WingMainPanel` 客户端 UI/交互语义还原。
- 协议号与字段说明补齐。
- 相关配置、资源与数据结构校验。
- 相关类和方法注释补足。

### 5.5 第四阶段：孩子系统端对端同步修复

**目标：** 对 `Baby` 模块实施与 `Wing` 同级别的端对端修复。

### 范围
- `Baby.java` 实体语义修复。
- `IBabyService.java`、`BabyMapper.xml` 与使用链路同步修复。
- 客户端孩子属性面板与协议展示同步校正。
- 对相关类、SQL、协议字段补注释。

### 5.6 第五阶段：横向清理与注释补全

**目标：** 在核心模块修完后，做全局一致性修复。

### 范围
- 批量清理不当响应内容。
- 批量补充异常处理。
- 批量消除明显反编译参数名。
- 对修复范围代码做注释补齐，确保达到计划要求。

### 5.7 第六阶段：迭代优化

**目标：** 只有在前述阶段稳定后，才进入结构性优化与可维护性改造。

### 范围
- 命名统一。
- 冗余逻辑压缩。
- 公共能力抽取。
- 配置分层。
- 数据初始化脚本整理。

---

## 6. 执行准则

- **代码真相优先：** 任何文档结论都必须服从代码现状。
- **安全优先于重构：** 先去危险逻辑，再谈优雅结构。
- **结构冻结优先：** 第一阶段不做大范围目录和类结构调整。
- **端对端闭环优先：** 每个模块都必须双端同步修，不允许单端完成。
- **注释与修复同步：** 不允许“代码先改完，注释以后再补”。
- **不猜业务语义：** 无法确认的地方先写行为注释并保留待确认标记。

---

## 7. 完成判定标准

某阶段或某模块要被标记为“已完成”，必须同时满足以下条件：

1. 影响范围已明确。
2. 双端代码已同步修复。
3. 数据库/Mapper/配置已同步修复。
4. 高危逻辑已被移除或替换为安全实现。
5. 修复范围的注释覆盖率达到要求。
6. 文档状态与代码状态一致。

只满足其中部分条件时，统一标记为“进行中”，不得提前宣布完成。

---

## 8. 后续维护方式

- 以后所有修复任务都应从本计划拆分，不再新增与其冲突的“并行总计划”。
- 若后续需要阶段报告，只记录“实际完成项、剩余项、阻塞项”，不写未经验证的完成结论。
- 若计划发生变化，直接更新本文档，而不是再新建一份新的“总计划”。

---

## 9. 当前执行记录

### 9.0 扫描附录
- 双端反编译点对点扫描结果详见：`附录A-双端反编译点对点扫描结果-2026-03-11.md`

### 2026-03-10 第一阶段第一轮已完成项

#### 9.1 双端危险执行链路清理
- 已禁用客户端 `.dat` 直接执行链路。
- 已禁用客户端 `.dll` 直接执行链路。
- 已禁用两条自动下载并执行 EXE/BAT 的登录器更新链路。
- 已禁用客户端直接执行系统命令清理临时目录的逻辑。

#### 9.2 服务端授权与追踪逻辑清理
- 已移除 `AllServiceUtil` 的时间炸弹行为。
- 已关闭 `GameServer` 与 `RefreshMonsterTask` 中的固定授权时间门槛。
- 已移除通过 `wmic` 读取 CPU 序列号的硬件追踪逻辑。

#### 9.3 双端加密修复
- 已统一客户端主链路 AES 密钥。
- 已统一服务端主链路 AES 密钥。
- 已统一服务端主动推送链路与客户端对应解密链路的 AES 密钥。

#### 9.4 配置与数据库映射修复
- 已将服务端数据库配置改为本地占位值，移除仓库内历史真实连接信息。
- 已新增 `important.properties.example` 作为本地配置模板。
- 已修复一组确认存在的 MyBatis `form dual` 显性错误。

#### 9.5 注释执行要求
- 第一阶段修复涉及文件已同步补充行为性注释。
- 后续继续按“修一处、注一处、验一处、提一处”推进。

### 2026-03-10 第一阶段第二轮已完成项

#### 9.6 服务端接口拒绝响应规范化
- 已批量修复服务端后台接口中的不当拒绝响应文本。
- 已将相关接口统一替换为标准 `403 + JSON` 返回格式。
- 已同步为该分支补充安全修复注释，便于后续代码审查与继续清理。

#### 9.7 当前状态
- 第一阶段中的高危执行链路与一组显性接口响应问题已完成首轮收口。
- 下一步继续转向更深层的双端协议、模块语义与可运行性修复。

### 2026-03-10 第三阶段样板模块推进记录

#### 9.8 Wing 双端语义修复（首轮）
- 已为服务端 `WingCompose` 恢复一组明确的业务语义入口名称。
- 已保留 `type31` 至 `type36` 的兼容入口，避免旧协议分发链路失效。
- 已将 `SuitComposeAction` 分发调用切换到语义化方法名。
- 已为客户端 `WingMainPanel` 补充右侧页签语义常量与关键方法注释。

#### 9.9 当前判定
- `Wing` 模块的**命名语义与界面页签语义**已完成首轮还原。
- `Wing` 模块尚未完成全部端到端修复，后续还需要继续核对资源、协议、数据与更多交互逻辑。

### 2026-03-10 第四阶段样板模块推进记录

#### 9.10 Baby 双端语义修复（首轮）
- 已为服务端与客户端 `Baby` 实体补充装备槽位、天资操作的语义化方法名。
- 已保留 `getpart`、`getpartAll`、`ChangePart`、`ChangeTalent` 的兼容入口，避免旧链路失效。
- 已将一组核心调用点切换为语义化方法名，包括服务端 `UseBabyAction` 与客户端孩子装备相关逻辑。
- 已修正 `IBabyService`、`BabyMapper` 中的反编译参数命名，并补充关键行为注释。

#### 9.11 当前判定
- `Baby` 模块的**实体语义、接口参数语义与部分关键调用点**已完成首轮修复。
- `Baby` 模块后续仍需继续核对更多客户端界面逻辑、Mapper 映射与业务注释覆盖率。

### 2026-03-10 第一阶段第三轮安全收口记录

#### 9.12 后台管理校验配置收敛
- 已将后台管理接口使用的参数名与校验值收敛到 `ApiValid` 统一入口。
- 已新增 `important.properties` 与示例配置中的后台管理校验项。
- 已将一组 Servlet 中分散的硬编码字符串替换为统一常量引用，降低后续调整与审计成本。

#### 9.13 当前判定
- 第一阶段中的“硬编码后台校验串”问题已完成首轮收口。
- 后续仍需继续检查更多管理接口、支付接口与外部依赖配置的可维护性与安全性。

### 2026-03-10 第一阶段第四轮安全收口记录

#### 9.14 服务端签名盐配置化
- 已将 `GameServer.signNum` 增加为可通过配置覆盖的安全项。
- 已在 `important.properties` 与示例配置中补充对应键值。
- 当前仍保留旧默认值作为兼容兜底，避免直接破坏旧请求链路。

#### 9.15 ApiValid 链路稳健性修复
- 已为 `ApiValid.getToken` 与 `ApiValid.vaildToken` 增加空值保护。
- 已为 Redis `Jedis` 连接补充显式关闭逻辑，减少后台认证链路的资源泄漏风险。

### 2026-03-10 第一阶段第五轮安全收口记录

#### 9.16 外部服务与短信配置收敛
- 已新增统一运行时配置读取工具，用于替代分散的外部服务硬编码地址。
- 已将服务端短信网关 URL、账号、密码与内部 API 地址转入配置读取逻辑。
- 已移除客户端直连第三方短信网关的能力，避免把短信平台凭据继续暴露到客户端。

### 2026-03-11 第五阶段样板模块推进记录

#### 9.17 Skill 双端语义修复（首轮）
- 已移除客户端 `Skill` 类中的反编译头部标记，并补充类级行为说明。
- 已为客户端 `Skill` 补充一组安全解析辅助方法，减少界面与公式代码中的重复字符串转数字逻辑。
- 已为双端 `Skill` 增加 `skillralation` 的语义化别名方法，明确其表示技能互斥/关联关系。
- 已将一组高频客户端技能面板和公式计算代码切换到新的类型安全接口。

#### 9.18 当前判定
- `Skill` 模块的**双端实体语义与客户端高频解析调用**已完成首轮收口。
- 后续仍需继续检查技能协议、技能面板、宠物技能链路与更多技能公式代码。

### 2026-03-11 第六阶段样板模块推进记录

#### 9.19 RoleSummoning 双端实体修复（首轮）
- 已为服务端 `RoleSummoning` 恢复 `getSI2`、`getLX`、`SB` 的语义化别名与说明。
- 已为客户端 `RoleSummoning` 恢复 `Splice`、`ChangePart`、`getLX` 的语义化别名与说明。
- 已将客户端饰品装备面板的一组直接调用切换到新别名方法。

#### 9.20 当前判定
- `RoleSummoning` 模块的**双端实体层反编译命名**已完成首轮收口。
- 后续仍需继续核对召唤兽装备、技能、灵犀、战斗状态与更多客户端调用链。
