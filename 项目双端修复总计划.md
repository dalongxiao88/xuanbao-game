# 项目双端修复总计划

**创建日期：** 2026-03-10  
**适用范围：** `GameClient/`、`Gameserver/`、`数据库/`  
**文档定位：** 本文档为当前仓库唯一权威修复计划，后续修复、验收、注释补充与阶段推进均以本文档为准。

---

## 1. 文档治理

### 1.1 保留文档
- `README.md`：仓库入口说明。
- `项目双端修复总计划.md`：唯一权威修复计划。

### 1.2 清理文档
以下文档已不再作为执行依据，应从仓库根目录清理，原因是存在**状态失真、互相矛盾、阶段结论未经代码验证**等问题：

- `Master_Restoration_Plan.md`
- `Security_Analysis_Report.md`
- `Stage1修复跟踪文档.md`
- `Stage1收尾任务文档.md`
- `Stage1收尾执行进度报告.md`
- `下一步修复计划.md`
- `二次验证报告.md`
- `修复工作验证报告.md`
- `修复进度报告.md`
- `反编译代码分析报告.md`
- `文档对比分析与补充建议.md`
- `新建文本文档.txt`

### 1.3 文档使用规则
- 以后新增的阶段性记录不得替代本计划文档。
- 若需要补充分析，只能作为附录或补充说明，且必须明确标注日期、范围与验证结论。
- 任何“已完成”结论必须以代码现状、双端链路和可运行性验证为依据，不能仅根据推断或局部修改宣布完成。

---

## 2. 修复总目标

本项目当前处于**反编译代码清理与高危逻辑排雷阶段**，总目标不是立即重构，而是先建立一套可持续修复的稳定基线。

### 2.1 阶段性总目标
1. **先清恶意代码与危险逻辑**，确保项目具备继续修复的安全前提。
2. **先保持原始结构不变**，避免在反编译项目中因结构重排导致调用链失真。
3. **按模块做双端同步修复**，保证服务端、客户端、协议、配置、数据库映射始终一致。
4. **补足缺失注释**，使后续维护者能基于代码真实行为继续推进迭代。
5. **在基线稳定后再做迭代优化**，包括命名优化、冗余清理、模块拆分与结构改善。

---

## 3. 两个核心原则

### 3.1 核心一：端对端同时修复

任何模块修复必须采用“端对端同步”策略，禁止只改一端就宣告完成。

### 模块完成的最低条件
某个模块只有在以下链路全部对齐后，才可以从“修复中”变为“已完成”：

- **服务端逻辑已修复**：核心业务代码、动作类、服务类、实体类已校正。
- **客户端逻辑已修复**：界面、交互、协议触发、展示逻辑与服务端一致。
- **协议/常量已对齐**：协议号、字段含义、加密方式、参数结构没有双端漂移。
- **数据库与映射已对齐**：实体、Mapper、XML、初始化脚本与业务字段一致。
- **配置与资源已对齐**：密钥、地址、版本、资源文件、更新机制不再互相冲突。
- **注释与文档已同步**：修复涉及的核心类、方法、字段和 SQL 已补充说明。

### 端对端修复的执行顺序
每次修一个模块，统一按下面的顺序推进：

1. 确认服务端入口与调用链。
2. 确认客户端入口与对应 UI/事件链。
3. 确认协议与字段映射。
4. 确认数据库实体、Mapper、脚本影响面。
5. 完成双端代码修复。
6. 为修复范围补充注释。
7. 做模块级验证并更新计划状态。

### 3.2 核心二：注释覆盖率达到 75% 以上

### 注释目标
- 所有**新修复代码**必须同步补充注释。
- 所有**修复涉及到的老代码文件**，若注释明显缺失，必须在本次修复中补齐到可维护状态。
- 注释必须基于代码**实际行为**与**可验证逻辑**编写，不允许凭空猜测业务语义。

### 注释覆盖率统计口径
为避免把注释工作做成“逐行翻译”，本项目采用**结构化注释覆盖率**统计，而非简单的行数占比。

#### 计入统计的结构化单元
- 类、接口、枚举。
- 公开方法。
- 复杂私有方法（满足任一条件：分支较多、涉及协议/SQL、涉及外部资源、长度较长）。
- 关键字段、常量、配置项。
- 关键 SQL 块、协议号分支、危险逻辑分支。

#### 覆盖率要求
- **修复涉及文件：注释覆盖率不得低于 75%。**
- **新建文件或重度修复文件：注释覆盖率目标为 85% 以上。**
- 对暂时无法完全还原语义的反编译逻辑，允许先写“行为性注释”，明确其输入、输出、依赖与副作用。

### 注释编写规则
- 说明“做什么、为什么、影响什么”，不写空泛注释。
- 能确认业务语义时，写业务语义注释。
- 语义不完全确定时，写行为性注释，不强行猜名称。
- MyBatis XML、批量 SQL、协议分支、加密逻辑、危险执行路径必须写注释。

---

## 4. 当前已确认的高优先级问题

以下问题已通过代码现状确认，属于必须优先处理的范围：

### 4.1 客户端危险执行链路
- `GameClient/src/org/come/log/LanderJPanel.java`：执行 `.dat` 文件。
- `GameClient/src/org/come/log/UpdateMouslisten.java`：执行 `.dll` 文件。
- `GameClient/src/org/updateNew/UpdateNewFile.java`：下载并启动 `sdls.dll`、`Multiple.exe`、`gatU.bat`。

### 4.2 服务端高危限制与追踪逻辑
- `Gameserver/src/main/java/org/come/until/AllServiceUtil.java`：存在时间炸弹逻辑。
- `Gameserver/src/main/java/org/come/server/GameServer.java`：存在授权时间门槛与硬件 ID 获取逻辑。

### 4.3 双端加密与配置不一致
- 服务端与客户端 AES 密钥仍存在不一致与硬编码问题。
- 数据库地址、账号、密码等敏感配置仍直接保存在仓库内。

### 4.4 Mapper 与数据库初始化存在显性错误
- 多个 MyBatis XML 存在明显拼写/批量 SQL 问题。
- Oracle 初始化脚本仍是手工执行风格，缺少稳定的最小可运行说明。

---

## 5. 修复阶段规划

### 5.1 第零阶段：建立可信基线

**目标：** 停止继续被旧文档误导，建立“以代码为准”的执行基线。

### 输出物
- 清理历史误导性文档。
- 建立本计划文档。
- 明确当前高危文件与优先级。
- 明确注释覆盖率统计口径。

### 5.2 第一阶段：安全修复与结构冻结

**目标：** 在不调整原始项目结构的前提下，移除恶意代码、危险执行链路和明显的授权/追踪逻辑。

### 范围
- 客户端危险外部执行。
- 服务端时间炸弹、授权门槛、硬件追踪。
- 双端加密硬编码。
- 仓库内敏感配置脱敏。

### 约束
- 不做大规模重命名。
- 不迁移包结构。
- 不进行非必要 UI 重绘与功能改版。

### 5.3 第二阶段：最小可运行链路修复

**目标：** 在保留结构的前提下，把项目修到“至少能稳定验证关键链路”。

### 范围
- 修复显性 Mapper/XML 语法问题。
- 补齐最小配置样例。
- 梳理 Oracle 与 Redis 启动依赖。
- 确认服务端启动链路和客户端基础连接链路。

### 5.4 第三阶段：翅膀系统端对端同步修复

**目标：** 以 `Wing` 模块为样板，完整验证“服务端 + 客户端 + 协议 + 数据 + 注释”同步修复流程。

### 范围
- `WingCompose` 服务端业务语义还原。
- `WingMainPanel` 客户端 UI/交互语义还原。
- 协议号与字段说明补齐。
- 相关配置、资源与数据结构校验。
- 相关类和方法注释补足。

### 5.5 第四阶段：孩子系统端对端同步修复

**目标：** 对 `Baby` 模块实施与 `Wing` 同级别的端对端修复。

### 范围
- `Baby.java` 实体语义修复。
- `IBabyService.java`、`BabyMapper.xml` 与使用链路同步修复。
- 客户端孩子属性面板与协议展示同步校正。
- 对相关类、SQL、协议字段补注释。

### 5.6 第五阶段：横向清理与注释补全

**目标：** 在核心模块修完后，做全局一致性修复。

### 范围
- 批量清理不当响应内容。
- 批量补充异常处理。
- 批量消除明显反编译参数名。
- 对修复范围代码做注释补齐，确保达到计划要求。

### 5.7 第六阶段：迭代优化

**目标：** 只有在前述阶段稳定后，才进入结构性优化与可维护性改造。

### 范围
- 命名统一。
- 冗余逻辑压缩。
- 公共能力抽取。
- 配置分层。
- 数据初始化脚本整理。

---

## 6. 执行准则

- **代码真相优先：** 任何文档结论都必须服从代码现状。
- **安全优先于重构：** 先去危险逻辑，再谈优雅结构。
- **结构冻结优先：** 第一阶段不做大范围目录和类结构调整。
- **端对端闭环优先：** 每个模块都必须双端同步修，不允许单端完成。
- **注释与修复同步：** 不允许“代码先改完，注释以后再补”。
- **不猜业务语义：** 无法确认的地方先写行为注释并保留待确认标记。

---

## 7. 完成判定标准

某阶段或某模块要被标记为“已完成”，必须同时满足以下条件：

1. 影响范围已明确。
2. 双端代码已同步修复。
3. 数据库/Mapper/配置已同步修复。
4. 高危逻辑已被移除或替换为安全实现。
5. 修复范围的注释覆盖率达到要求。
6. 文档状态与代码状态一致。

只满足其中部分条件时，统一标记为“进行中”，不得提前宣布完成。

---

## 8. 后续维护方式

- 以后所有修复任务都应从本计划拆分，不再新增与其冲突的“并行总计划”。
- 若后续需要阶段报告，只记录“实际完成项、剩余项、阻塞项”，不写未经验证的完成结论。
- 若计划发生变化，直接更新本文档，而不是再新建一份新的“总计划”。

---

## 9. 当前执行记录

### 9.0 扫描附录
- 双端反编译点对点扫描结果详见：`附录A-双端反编译点对点扫描结果-2026-03-11.md`

### 2026-03-10 第一阶段第一轮已完成项

#### 9.1 双端危险执行链路清理
- 已禁用客户端 `.dat` 直接执行链路。
- 已禁用客户端 `.dll` 直接执行链路。
- 已禁用两条自动下载并执行 EXE/BAT 的登录器更新链路。
- 已禁用客户端直接执行系统命令清理临时目录的逻辑。

#### 9.2 服务端授权与追踪逻辑清理
- 已移除 `AllServiceUtil` 的时间炸弹行为。
- 已关闭 `GameServer` 与 `RefreshMonsterTask` 中的固定授权时间门槛。
- 已移除通过 `wmic` 读取 CPU 序列号的硬件追踪逻辑。

#### 9.3 双端加密修复
- 已统一客户端主链路 AES 密钥。
- 已统一服务端主链路 AES 密钥。
- 已统一服务端主动推送链路与客户端对应解密链路的 AES 密钥。

#### 9.4 配置与数据库映射修复
- 已将服务端数据库配置改为本地占位值，移除仓库内历史真实连接信息。
- 已新增 `important.properties.example` 作为本地配置模板。
- 已修复一组确认存在的 MyBatis `form dual` 显性错误。

#### 9.5 注释执行要求
- 第一阶段修复涉及文件已同步补充行为性注释。
- 后续继续按“修一处、注一处、验一处、提一处”推进。

### 2026-03-10 第一阶段第二轮已完成项

#### 9.6 服务端接口拒绝响应规范化
- 已批量修复服务端后台接口中的不当拒绝响应文本。
- 已将相关接口统一替换为标准 `403 + JSON` 返回格式。
- 已同步为该分支补充安全修复注释，便于后续代码审查与继续清理。

#### 9.7 当前状态
- 第一阶段中的高危执行链路与一组显性接口响应问题已完成首轮收口。
- 下一步继续转向更深层的双端协议、模块语义与可运行性修复。

### 2026-03-10 第三阶段样板模块推进记录

#### 9.8 Wing 双端语义修复（首轮）
- 已为服务端 `WingCompose` 恢复一组明确的业务语义入口名称。
- 已保留 `type31` 至 `type36` 的兼容入口，避免旧协议分发链路失效。
- 已将 `SuitComposeAction` 分发调用切换到语义化方法名。
- 已为客户端 `WingMainPanel` 补充右侧页签语义常量与关键方法注释。

#### 9.9 当前判定
- `Wing` 模块的**命名语义与界面页签语义**已完成首轮还原。
- `Wing` 模块尚未完成全部端到端修复，后续还需要继续核对资源、协议、数据与更多交互逻辑。

### 2026-03-10 第四阶段样板模块推进记录

#### 9.10 Baby 双端语义修复（首轮）
- 已为服务端与客户端 `Baby` 实体补充装备槽位、天资操作的语义化方法名。
- 已保留 `getpart`、`getpartAll`、`ChangePart`、`ChangeTalent` 的兼容入口，避免旧链路失效。
- 已将一组核心调用点切换为语义化方法名，包括服务端 `UseBabyAction` 与客户端孩子装备相关逻辑。
- 已修正 `IBabyService`、`BabyMapper` 中的反编译参数命名，并补充关键行为注释。

#### 9.11 当前判定
- `Baby` 模块的**实体语义、接口参数语义与部分关键调用点**已完成首轮修复。
- `Baby` 模块后续仍需继续核对更多客户端界面逻辑、Mapper 映射与业务注释覆盖率。

### 2026-03-10 第一阶段第三轮安全收口记录

#### 9.12 后台管理校验配置收敛
- 已将后台管理接口使用的参数名与校验值收敛到 `ApiValid` 统一入口。
- 已新增 `important.properties` 与示例配置中的后台管理校验项。
- 已将一组 Servlet 中分散的硬编码字符串替换为统一常量引用，降低后续调整与审计成本。

#### 9.13 当前判定
- 第一阶段中的“硬编码后台校验串”问题已完成首轮收口。
- 后续仍需继续检查更多管理接口、支付接口与外部依赖配置的可维护性与安全性。

### 2026-03-10 第一阶段第四轮安全收口记录

#### 9.14 服务端签名盐配置化
- 已将 `GameServer.signNum` 增加为可通过配置覆盖的安全项。
- 已在 `important.properties` 与示例配置中补充对应键值。
- 当前仍保留旧默认值作为兼容兜底，避免直接破坏旧请求链路。

#### 9.15 ApiValid 链路稳健性修复
- 已为 `ApiValid.getToken` 与 `ApiValid.vaildToken` 增加空值保护。
- 已为 Redis `Jedis` 连接补充显式关闭逻辑，减少后台认证链路的资源泄漏风险。

### 2026-03-10 第一阶段第五轮安全收口记录

#### 9.16 外部服务与短信配置收敛
- 已新增统一运行时配置读取工具，用于替代分散的外部服务硬编码地址。
- 已将服务端短信网关 URL、账号、密码与内部 API 地址转入配置读取逻辑。
- 已移除客户端直连第三方短信网关的能力，避免把短信平台凭据继续暴露到客户端。

### 2026-03-11 第五阶段样板模块推进记录

#### 9.17 Skill 双端语义修复（首轮）
- 已移除客户端 `Skill` 类中的反编译头部标记，并补充类级行为说明。
- 已为客户端 `Skill` 补充一组安全解析辅助方法，减少界面与公式代码中的重复字符串转数字逻辑。
- 已为双端 `Skill` 增加 `skillralation` 的语义化别名方法，明确其表示技能互斥/关联关系。
- 已将一组高频客户端技能面板和公式计算代码切换到新的类型安全接口。

#### 9.18 当前判定
- `Skill` 模块的**双端实体语义与客户端高频解析调用**已完成首轮收口。
- 后续仍需继续检查技能协议、技能面板、宠物技能链路与更多技能公式代码。

### 2026-03-11 第六阶段样板模块推进记录

#### 9.19 RoleSummoning 双端实体修复（首轮）
- 已为服务端 `RoleSummoning` 恢复 `getSI2`、`getLX`、`SB` 的语义化别名与说明。
- 已为客户端 `RoleSummoning` 恢复 `Splice`、`ChangePart`、`getLX` 的语义化别名与说明。
- 已将客户端饰品装备面板的一组直接调用切换到新别名方法。

#### 9.20 当前判定
- `RoleSummoning` 模块的**双端实体层反编译命名**已完成首轮收口。
- 后续仍需继续核对召唤兽装备、技能、灵犀、战斗状态与更多客户端调用链。

### 2026-03-11 第七阶段样板模块推进记录

#### 9.21 Scene 双端基础接口修复（首轮）
- 已为服务端 `Scene` 接口恢复一组语义化参数名。
- 已为服务端 `Scene` 接口补充默认语义别名方法，保持实现类兼容。
- 已为客户端 `Scene` 接口恢复一组语义化参数名，并补充默认别名方法。
- 已为客户端场景接口增加 `SCENE_ID_DNTG` 语义别名常量。

#### 9.22 当前判定
- `Scene` 模块的**双端基础接口层**已完成首轮修复。
- 后续仍需继续检查服务端各场景实现类与客户端 `DNTGScene` 的具体行为语义。

### 2026-03-11 第八阶段服务端数据链路推进记录

#### 9.23 UserTable / RoleTable 核心接口参数修复（首轮）
- 已对 `IUserTableService`、`IRoleTableService` 的一组高频方法参数名进行语义化修复。
- 已对 `UsertableMapper`、`RoleTableMapper` 的一组核心数据访问方法参数名进行语义化修复。
- 当前优先覆盖了登录、账号、角色、封禁、充值查询等主链路方法。

#### 9.24 当前判定
- 服务端主数据链路的**首批高频 `p0/p1` 参数**已开始收口。
- 后续仍需继续完成 `IRoleTableService` 后半段、`RoleTableMapper` 后半段以及 `Goodstable` 等高频接口的同类清理。

### 2026-03-11 第九阶段服务端数据链路推进记录

#### 9.25 Goodstable 核心接口参数修复（首轮）
- 已对 `IGoodsTableService` 的高频方法参数名进行语义化修复。
- 已对 `GoodstableMapper` 的高频方法参数名进行语义化修复。
- 当前优先覆盖了物品查询、背包索引、物品数量变更、商城与闲鱼交易查询等主链路方法。

#### 9.26 当前判定
- 服务端物品主链路的**首批高频 `p0/p1` 参数**已完成收口。
- 后续仍需继续沿着客户端背包、物品面板、摆摊与交易界面做更细的点对点对位修复。

### 2026-03-11 第十阶段双端物品实体推进记录

#### 9.27 Goodstable 双端实体修复（首轮）
- 已为服务端 `Goodstable` 增加物品使用次数扣减、记录 ID、角色 ID、物品模板 ID、基础装备解析等语义化别名方法。
- 已为客户端 `Goodstable` 增加使用次数扣减、对象复制、角色 ID、物品模板 ID 等语义化别名方法。
- 已将一组客户端精炼/点粹面板的 `TH` 调用补充为 `copyFrom` 兼容别名链路。

#### 9.28 当前判定
- `Goodstable` 模块的**双端实体层与精炼面板链路**已完成首轮收口。
- 后续仍需继续检查 `GoodsListFromServerUntil`、`AssetControl`、摆摊与交易界面的更深层调用链。

#### 9.29 GoodsList 主链路命名收口（首轮）
- 已为 `GoodsListFromServerUntil` 补充一组语义化别名方法，例如批量加入物品、按类型查询物品、按记录 ID 删除物品、按记录 ID 消耗物品。
- 已将客户端大量 `Deletebiaoid` 调用切换到 `deleteByRgid` 语义化别名。

#### 9.30 AssetControl 资产同步链路收口（首轮）
- 已为 `AssetControl.asset(...)` 增加入口行为注释。
- 已将资产同步主分支中的一组高频反编译局部变量恢复为可读命名。
- 当前优先清理了金币、仙玉、存款、通宝、贡献、PK 记录、摆摊状态等主分支变量。

### 2026-03-11 第十一阶段样板模块推进记录

#### 9.31 Lingbao 双端实体修复（首轮）
- 已为服务端 `Lingbao` 补充类级说明与一组核心语义化别名方法。
- 已为客户端 `Lingbao` 补充类级说明，并修正 `getResistshop` 这类异常命名的语义别名。
- 当前优先收口了获得途径、灵宝类型、灵宝活跃值、抗落宝属性等基础字段命名。

#### 9.32 当前判定
- `Lingbao` 模块的**双端实体层**已完成首轮命名收口。
- 后续仍需继续检查灵宝面板、灵宝技能、符石与战斗链路的更深层调用与注释覆盖率。

#### 9.33 Lingbao 服务端接口与客户端面板调用收口（首轮）
- 已对 `ILingbaoService`、`LingbaoMapper` 的核心参数名进行语义化修复。
- 已将客户端一组灵宝面板、灵法、鼠标监听与物品操作入口切换到新的语义化 getter。
- 当前已消除客户端外部调用中 `getBaotype/getBaoactive/getResistshop` 这组旧命名残留。

#### 9.34 Lingbao 高阶字段别名收口（首轮）
- 已为双端 `Lingbao` 增加 `fushis / tianfuskill / skillsum / fusum` 的语义化别名。
- 已将客户端外部调用中的 `getFushis/getFusum/getTianfuskill` 切换到新别名方法。

### 2026-03-11 第十二阶段召唤兽服务链推进记录

#### 9.35 RoleSummoning 服务端接口参数修复（首轮）
- 已对 `IRoleSummoningService` 的核心参数名进行语义化修复。
- 已对 `RoleSummoningMapper` 的核心参数名进行语义化修复。
- 当前优先覆盖了召唤兽查询、索引变更、Redis 更新、批量同步与后台列表查询链路。

### 2026-03-11 第十三阶段双端公共接口推进记录

#### 9.36 FromServerAction / CreateTextUtil 双端收口（首轮）
- 已为双端 `FromServerAction` 接口补充说明并恢复语义化参数名。
- 已为双端 `CreateTextUtil` 补充方法说明，并增加 `readContentStreaming` 语义化别名。
- 已移除服务端旧 `CreateTextUtil` 中的调试输出，减少运行噪音。

#### 9.37 AESUtil 双端工具收口（首轮）
- 已为双端 `AESUtil` 补充类级说明。
- 已为服务端 `AESUtil` 增加空消息保护并统一 `UTF-8` 编码处理。
- 已清理双端 `hex2byte` 中的旧式装箱写法，减少反编译遗留噪音。

#### 9.42 GZip 双端工具收口（首轮）
- 已为双端 `GZip` 补充类级说明与核心方法说明。
- 已清理客户端 `GZip` 中的异常变量占位命名。

### 2026-03-11 第十四阶段双端飞行器链路推进记录

#### 9.38 Fly 双端实体与服务接口修复（首轮）
- 已为双端 `Fly` 实体补充类级说明与一组语义化别名方法。
- 已修复客户端 `getflyskin` / `setflyskin` 这类异常命名的语义别名。
- 已对 `IFlyService` 与 `FlyMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第十五阶段双端坐骑链路推进记录

#### 9.39 Mount 双端实体与服务接口修复（首轮）
- 已为双端 `Mount` 实体补充类级说明与一组语义化别名方法。
- 已补充坐骑模板 ID、坐骑等级等常用字段的语义化别名。
- 已对 `IMountService` 与 `MountMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第十六阶段双端座驾链路推进记录

#### 9.41 Car 双端实体与服务接口修复（首轮）
- 已为双端 `Car` 实体补充类级说明与一组语义化别名方法。
- 已补充座驾模板 ID、座驾等级等常用字段的语义化别名。
- 已对 `ICarService` 与 `CarMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第十七阶段双端好友链路推进记录

#### 9.42 Friend 双端实体与服务接口修复（首轮）
- 已为双端 `Friend` 实体补充类级说明与角色 ID / 好友角色 ID 的语义化别名。
- 已对 `IFriendService` 与 `FriendMapper` 的核心参数名进行语义化修复。

#### 9.43 Friendtable 双端展示对象修复（首轮）
- 已为双端 `Friendtable` 补充类级说明与角色/种族相关字段的语义化别名。
- 已对 `IFriendtableService` 与 `FriendtableMapper` 的核心参数名进行语义化修复。

#### 9.46 好友结果链路入口收口（首轮）
- 已将客户端 `FriendServerControl` 切换到 `FriendResultBean` 的语义化别名方法。

### 2026-03-11 第十八阶段双端公会申请链路推进记录

#### 9.47 Gangapplytable / GangResultBean 收口（首轮）
- 已为双端 `Gangapplytable` 补充类级说明与申请角色 ID、种族名称、帮派 ID 的语义化别名。
- 已对 `IGangapplyService` 与 `GangapplyMapper` 的核心参数名进行语义化修复。
- 已为双端 `GangResultBean` 增加成员列表与申请列表的语义化别名。
- 已将客户端 `FactionBtn`、`FactionMemberJpanel` 的一组高频调用切换到新别名方法。

#### 9.53 Gang / GangGroup 双端主体收口（首轮）
- 已为双端 `Gang` 补充帮派 ID、帮派名称的语义化别名。
- 已为双端 `GangGroup` 补充驯养师等级、科技等级、剩余驯养次数等语义化别名。

#### 9.55 公会面板高频调用收口（首轮）
- 已将 `FactionBtn`、`GangChangeControl`、`FactionPandectJpanel` 中的一组公会成员/概览调用切换到语义化别名。

### 2026-03-11 第十九阶段双端消息链路推进记录

#### 9.48 Message 双端实体与服务接口修复（首轮）
- 已为双端 `Message` 实体补充类级说明与消息 ID、角色 ID、消息内容、消息时间的语义化别名。
- 已对 `IMessageService` 与 `MessageMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第二十阶段双端收藏链路推进记录

#### 9.49 Collection 双端实体与服务接口修复（首轮）
- 已为双端 `Collection` 实体补充类级说明与购买角色 ID、对方角色 ID、出售类型、角色 ID 的语义化别名。
- 已对 `ICollectionService` 与 `CollectionMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第二十一阶段双端出售商品链路推进记录

#### 9.50 Salegoods 双端实体与服务接口修复（首轮）
- 已为双端 `Salegoods` 实体补充类级说明与角色/购买方/条件类型等字段的语义化别名。
- 已对 `ISalegoodsService` 与 `SalegoodsMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第二十三阶段玄宝服务链推进记录

#### 9.58 XuanBao 服务接口修复（首轮）
- 已对 `IXuanBaoService` 与 `xuanbaoMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第二十四阶段版本与记录链路推进记录

#### 9.61 AppVersionService / AppVersionMapper 修复（首轮）
- 已对 `AppVersionService` 与 `AppVersionMapper` 的核心参数名进行语义化修复。

#### 9.59 XuanBao 双端实体与面板 getter 收口（首轮）
- 已为双端 `XuanBao` 实体补充类型、品质、玄宝 ID、符石、角色 ID、等级、玄蕴值等语义化别名。
- 已将客户端玄宝面板高频旧 getter 调用切换到语义化别名。

#### 9.44 双端展示 Bean / 结果对象收口（首轮）
- 已为 `FriendResultBean`、`LingbaoBean`、`LingbaoResultBean`、`LingBaoEquipment` 补充类级说明。
- 已为这组双端对象补充一组语义化 getter/setter 别名，降低协议层和界面层的理解成本。

#### 9.45 客户端摆摊抽象层收口（首轮）
- 已为 `Commodity`、`BuyBox`、`SpotBox`、`SpotJpanel`、`SpotStallBaseJpanel` 补充说明与语义化参数名。
- 已为服务端 `come.tool.Stall.Commodity` 补充类级说明。

### 2026-03-11 第二十二阶段宠物技能链路推进记录

#### 9.51 UsePetAction / 宠物技能互斥链路收口（首轮）
- 已将服务端 `UsePetAction` 中高频的 `getSI2` 调用切换到 `getFourAttributeValue` 语义别名。
- 已将服务端 `UsePetAction` 中技能互斥关系访问切换到 `getSkillRelation` 语义别名。
- 已同步将客户端 `LxPanel` 与 `GoodsMouslisten` 中的技能互斥关系访问切换到 `getSkillRelation`。

#### 9.54 宠物悟灵技能命名收口（首轮）
- 已为双端 `RoleSummoning` 增加 `getPetWuLingSkills / setPetWuLingSkills` 语义化别名。
- 已将服务端 `UsePetAction` 与客户端悟灵/宠物技能面板中的高频 `petSkillswl` 调用切换到新别名。

#### 9.40 Mount 客户端高频调用收口（首轮）
- 已将一组坐骑面板、升级显示与内丹属性链路切换到语义化别名方法。
- 当前优先覆盖了 `MountPanelBtn`、`MountSkillsJpanel`、`ExpIncreaseUntil`、`AccessNedanMsgUntil` 这条高频客户端链路。

#### 9.47 MountSkill 双端实体修复（首轮）
- 已为双端 `MountSkill` 实体补充类级说明与技能 ID / 坐骑 ID 的语义化别名。

#### 9.51 MountSkill 服务接口修复（首轮）
- 已对 `IMountskillService` 与 `MountskillMapper` 的核心参数名进行语义化修复。

#### 9.57 MountShouhu / carJPanel 界面收口（首轮）
- 已将 `MountShouhuJpanel` 与 `carJPanel` 中的高频载具旧 getter 调用切换到语义化别名。

#### 9.60 XuanBao 客户端高频界面收口（首轮）
- 已将 `GoodsMsgJpanel` 中剩余的玄宝旧 getter 调用切换到语义化别名。

### 2026-03-11 第二十五阶段物品记录链路推进记录

#### 9.62 Goodsrecord 服务接口修复（首轮）
- 已对 `IGoodsrecordService` 与 `GoodsrecordMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第二十六阶段重点商品记录链路推进记录

#### 9.63 Importantgoodtrcord 服务接口修复（首轮）
- 已对 `IImportantgoodtrcordService` 与 `ImportantgoodtrcordMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第二十七阶段收支记录链路推进记录

#### 9.64 ExpensesReceiptsService / Mapper 修复（首轮）
- 已对 `ExpensesReceiptsService` 与 `ExpensesReceiptsMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第三十阶段背包记录链路推进记录

#### 9.68 PackRecord 服务接口与双端类说明修复（首轮）
- 已对 `IPackRecordService` 与 `PackRecordMapper` 的核心参数名进行语义化修复。
- 已为双端 `PackRecord` 补充类级说明，明确其是背包扩展记录对象。

### 2026-03-11 第二十九阶段物品兑换链路推进记录

#### 9.66 GoodsExchange / Goodsexchange 对象修复（首轮）
- 已为服务端 `GoodsExchange`、`Goodsexchange` 和客户端 `GoodsExchange` 补充类级说明与核心语义化别名。

#### 9.67 IGoodsExchangeService / GoodsexchangeMapper 修复（首轮）
- 已对 `IGoodsExchangeService` 与 `GoodsexchangeMapper` 的核心参数名进行语义化修复。

### 2026-03-11 第二十八阶段角色订单链路推进记录

#### 9.65 Roleorder 双端实体与服务接口修复（首轮）
- 已为双端 `Roleorder` 实体补充类级说明与订单 ID、购买时间、角色 ID、订单号等语义化别名。
- 已对 `IRoleorderService` 与 `RoleorderMapper` 的核心参数名进行语义化修复。
