# 项目双端修复总计划

**创建日期：** 2026-03-10  
**最新回写日期：** 2026-03-13  
**适用范围：** `GameClient/`、`Gameserver/`、`数据库/`  
**文档定位：** 本文档是当前仓库唯一权威修复计划与状态文档。后续修复、验证、回写和阶段推进均以本文档为准。

---

## 1. 文档治理

### 1.1 核心文档

- `项目双端修复总计划.md`：唯一权威修复计划与当前状态文档。
- `项目执行规则.md`：执行约束文档，负责约束“开始前做什么、修复时怎么做、完成后怎么回写”。

### 1.2 辅助文档

当前根目录中保留以下辅助文档，但它们只作为当前剩余问题的补充说明，不替代主文档：

- `当前项目反编译残留审查报告-2026-03-12.md`
- `剩余反编译扫描总结-2026-03-11.md`
- `附录A-双端反编译点对点扫描结果-2026-03-11.md`
- `附录B-剩余反编译残留点位表-2026-03-13.md`
- `阶段修复总结-2026-03-12.md`

### 1.3 文档使用规则

- 每次开始任何任务前，必须先阅读 `项目双端修复总计划.md` 与 `项目执行规则.md`。
- 任何“已完成”结论都必须服从代码现状与验证结果，不能沿用旧文档口径。
- 已经修复完整并销账的反编译批次，不再继续保留为当前执行项。
- 后续若继续推进反编译修复，必须先复扫，再把**当前仍未完成的点位**补录到附录B。

---

## 2. 修复总目标

本项目当前仍处于**反编译结构残留收口、源码完整性复核与验证基线维护**阶段。

当前总目标不是继续回放历史批次，而是建立一套基于当前代码现状的稳定执行基线：

1. 清理当前仍未完成的结构性反编译残留。
2. 保持现有项目结构基本稳定，避免在反编译项目中因结构重排导致调用链失真。
3. 对需要双端联动的链路，继续按“服务端 + 客户端 + 协议 + 注释”同步推进。
4. 在反编译主线销账后，再推进更深层的功能回归与可维护性治理。

---

## 3. 核心原则

### 3.1 代码真相优先

- 任何判断必须服从当前代码现状。
- 若文档与代码冲突，以代码和最新验证结果为准。

### 3.2 安全优先于重构

- 先保证项目链路可控，再做大范围命名或结构优化。
- 当前阶段不做非必要的大范围目录迁移和类结构重排。

### 3.3 端对端闭环优先

- 影响双端链路、协议、Mapper、配置或资源映射的修改，不允许只改一端就判定完成。

### 3.4 注释与修复同步

- 修复涉及文件的结构化注释覆盖率不得低于 `75%`。
- 每个修复点都必须保留可追溯注释。

---

## 4. 当前高优先级问题

### 4.1 反编译修复当前状态

- 当前**不能**判定为“反编译残留清零”。
- 当前未发现可执行级硬残留：
  - `JD-Core Version`
  - `Method not decompiled`
  - `Code decompiled incorrectly`
  - `/* java... */`
  - 可执行 `this$N/access$N`
- 当前仍存在明确的结构性反编译残留：
  - `switch (xxx.hashCode())`：`103` 处，`60` 个文件
  - 占位/编号式命名残留：`14` 个文件
- 当前反编译主线执行清单详见：`附录B-剩余反编译残留点位表-2026-03-13.md`

### 4.2 构建与源码完整性

- 服务端已在当前工作区通过：
  - `Gameserver\mvn-local.cmd -DskipTests clean compile`
- 服务端 `package` 当前轮未做复核，完整打包链仍未确认。
- 客户端当前未发现可直接使用的 Maven/Gradle/Ant 构建入口，统一编译验证链仍缺失。
- `Gameserver/src/main/webapp/WEB-INF/web.xml` 中是否仍引用缺失源码，后续需单独复核。

### 4.3 其他高风险问题

以下问题不属于本轮反编译主线，但仍然是当前项目的真实风险：

- 认证、口令和权限模型仍需继续复核。
- 仓库卫生、敏感配置和静态安全参数仍需继续治理。
- 自动化测试基础仍然缺失。

---

## 5. 当前反编译修复批次

### 5.1 Batch-R01：服务端后台查询/排序链

当前目标：

- `PackControlServerlrt`
- `RoleSummoningRoleUserServlet`
- `MountRoleUserServlet`
- `LingBaoRoleUserServlet`
- `UserTableServiceImpl`

目标特征：

- `switch (xxx.hashCode())`

### 5.2 Batch-R02：服务端战斗/计算链

当前目标：

- `CalculationUtil`
- `Ql`
- `Battlefield`
- `ManData`
- `MixDeal`
- `UsePetAction`
- `UseLingAction`
- `HurtAction`

目标特征：

- 高频 `hashCode` 分派

### 5.3 Batch-R03：客户端翅膀/藏宝阁热点

当前目标：

- `WingMainPanel`
- `LHMainPanel`
- `WingUpStarMouseListener`
- `LHUpStarMouseListener`
- `TreasurePavilionSearchUntil`
- 相关藏宝阁筛选面板

目标特征：

- 高频 `hashCode` 分派

### 5.4 Batch-R04：客户端通用 UI/控制链

当前目标：

- `Util`
- `CutButtonImage`
- `ControlNpcXmlUntil`
- `AccessSuitMsgUntil`
- `AccessNedanMsgUntil`
- `AssetControl`
- `NPCJpanel`
- `MsgJapnel`
- `ShopPriceControl`

目标特征：

- `hashCode` 分派

### 5.5 Batch-R05：服务端编号入口收口

当前目标：

- `SuitComposeAction`
- `WingCompose`
- `StarCard`
- `NpcCompose`
- `GemCompose`
- `GemIntensify`
- `SuitPetEquip`
- `SuitPalEquip`

目标特征：

- `handleOperationN`

### 5.6 Batch-R06：双端占位类名/复制类名收口

当前目标：

- 双端 `XLPath`
- `CopyOfSkillTYCPanel`
- `CopyOfSkillTYCMuse`
- `GemMakeBtn.GemXXX`

目标特征：

- `class XXX`
- `recordXXX()`
- `CopyOf...`
- `GemXXX()`

---

## 6. 执行准则

- 开工前必须先阅读 `项目双端修复总计划.md` 与 `项目执行规则.md`。
- 每次修复都必须先确认服务端入口、客户端入口、协议、Mapper、配置和资源影响面。
- 每次修复都必须同步补注释。
- 每次修复完成后都必须回写本主文档和附录B。
- 未回写主文档和附录B的修复，一律不视为完成。

---

## 7. 完成判定标准

某个批次或某个模块只有同时满足以下条件，才可以标记为“已完成”：

1. 当前影响范围已明确。
2. 双端链路已完成必要同步修复。
3. 协议、Mapper、配置或资源映射已完成必要核对。
4. 当前批次对应的反编译结构已从代码中移除。
5. 注释覆盖率达到要求。
6. 本主文档与附录B已同步更新。

只满足部分条件时，只能标记为“进行中”。

---

## 8. 当前验证结果

### 8.1 已完成验证

- 服务端干净编译通过：
  - `Gameserver\mvn-local.cmd -DskipTests clean compile`
- 当前复扫未发现可执行级硬残留。

### 8.2 仍未完成验证

- 服务端 `package`
- 客户端统一编译验证
- 客户端启动与主要面板链路验证
- 双端端到端功能回归

---

## 9. 当前执行记录

### 9.1 2026-03-13 当前状态回写

- 已按当前代码现状重写根目录反编译相关文档。
- 已从文档中删除已经修复完整并销账的反编译批次与旧点位。
- 当前反编译主线只保留 `Batch-R01` ~ `Batch-R06` 六个未完成批次。

### 9.2 当前判断

- 当前项目的反编译问题已经从“硬残留”收缩到“结构性残留”。
- 下一轮执行不再沿用旧的“已清零”口径，也不再回放已经完成的批次记录。
- 后续所有反编译修复都必须基于附录B当前点位表继续推进。
