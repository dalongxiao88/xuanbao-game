# 当前项目反编译残留审查报告（2026-03-12 文件名保留，2026-03-13 按当前代码现状回写）

**最新复核日期：** 2026-03-13  
**审查范围：** `GameClient/src/`、`Gameserver/src/main/java/`  
**不计入范围：** `GameClient/jre/` 运行时、`Gameserver/src/main/webapp/` 第三方静态资源、数据库脚本、二进制资源、历史文档  
**文档定位：** 本文档只记录当前代码中仍能直接定位的反编译残留；已经修完并销账的点位不再保留在本文档中。

---

## 1. 复核结论

- 当前项目**不能**判定为“反编译残留清零”。
- 当前未发现可执行级硬残留：
  - `JD-Core Version`
  - `Method not decompiled`
  - `Code decompiled incorrectly`
  - `/* java... */` 类型注释
  - 可执行的 `access$N`
- `this$0` 关键词当前只出现在 `TRACE[...]` 注释中，不再出现在可执行代码里。
- 当前剩余问题全部属于**结构性反编译残留**，主要分两类：
  - `switch (xxx.hashCode())` 式字符串分派
  - 占位/编号式命名残留

---

## 2. 当前统计

| 类型 | 当前数量 | 说明 |
|---|---:|---|
| `switch (xxx.hashCode())` | `103` 处 | 分布在 `60` 个文件 |
| 占位/编号式命名残留 | `14` 个文件 | 包含 `XXX`、`CopyOf...`、`GemXXX`、`handleOperationN` |
| 可执行级硬残留 | `0` | 当前复扫未命中 |

补充说明：

- `TODO Auto-generated ...` 仍有 `140` 处，但这类更适合归为历史噪音，不纳入当前反编译主点位统计。
- 服务端仍是主要热点，尤其集中在后台查询链和战斗/计算链。

---

## 3. 当前未完成残留类型

### 3.1 服务端后台查询/排序链

代表文件：

- `Gameserver/src/main/java/org/come/servlet/PackControlServerlrt.java`
- `Gameserver/src/main/java/org/come/servlet/RoleSummoningRoleUserServlet.java`
- `Gameserver/src/main/java/org/come/servlet/MountRoleUserServlet.java`
- `Gameserver/src/main/java/org/come/servlet/LingBaoRoleUserServlet.java`
- `Gameserver/src/main/java/org/come/serviceImpl/UserTableServiceImpl.java`

当前问题：

- 排序码、状态码、分页筛选仍大量使用 `switch (xxx.hashCode())`。
- 这类代码不影响当前服务端编译，但仍保留明显反编译式结构。

### 3.2 服务端战斗/计算链

代表文件：

- `Gameserver/src/main/java/come/tool/Calculation/CalculationUtil.java`
- `Gameserver/src/main/java/come/tool/FightingData/Ql.java`
- `Gameserver/src/main/java/come/tool/FightingData/Battlefield.java`
- `Gameserver/src/main/java/come/tool/FightingData/ManData.java`
- `Gameserver/src/main/java/come/tool/FightingData/MixDeal.java`
- `Gameserver/src/main/java/come/tool/Good/UsePetAction.java`

当前问题：

- 字符串状态、品质、技能类型匹配仍大量使用 `hashCode` 分派。
- 这批文件规模大、链路深，仍是当前最重的结构性残留热区。

### 3.3 客户端翅膀/藏宝阁热点

代表文件：

- `GameClient/src/org/wing/panel/WingMainPanel.java`
- `GameClient/src/org/wing/panel/LHMainPanel.java`
- `GameClient/src/org/wing/mouseListener/WingUpStarMouseListener.java`
- `GameClient/src/org/wing/mouseListener/LHUpStarMouseListener.java`
- `GameClient/src/org/cbg/until/TreasurePavilionSearchUntil.java`

当前问题：

- 面板筛选、品质映射、属性分派仍保留反编译式 `hashCode` 分支。
- 虽然已有首轮语义修复，但还没有清到当前仓库真实余量。

### 3.4 双端占位/编号式命名

代表文件：

- `GameClient/src/come/tool/map/XLPath.java`
- `Gameserver/src/main/java/org/come/script/XLPath.java`
- `GameClient/src/org/skill/panel/CopyOfSkillTYCPanel.java`
- `GameClient/src/org/come/action/CopyOfSkillTYCMuse.java`
- `GameClient/src/com/tool/btn/GemMakeBtn.java`
- `Gameserver/src/main/java/org/come/action/suit/SuitComposeAction.java`
- `Gameserver/src/main/java/org/come/action/suit/WingCompose.java`
- `Gameserver/src/main/java/org/come/action/suit/StarCard.java`
- `Gameserver/src/main/java/org/come/action/suit/NpcCompose.java`

当前问题：

- 仍保留 `class XXX`、`recordXXX()`、`CopyOf...`、`GemXXX()`、`handleOperationN` 这类明显占位命名。
- 这类问题已经不属于“硬残留”，但仍应视为未完成的反编译代码收口。

---

## 4. 当前判断

- 之前几轮修复解决了“明确挂账”的一批点位，但**没有覆盖完整仓库范围**。
- 当前需要修的，不再是 `JD-Core`、`this$0`、反编译器注释这类硬残留，而是**结构性反编译代码**。
- 文档口径必须统一为：
  - 已完成销账的批次不再保留为当前执行项
  - 当前仍存在的结构性反编译残留必须重新挂账

---

## 5. 对主文档和附录B的同步要求

- 主文档不得再出现“当前源码范围内反编译残留已全部清零”的表述。
- 附录B不得继续保留已经销账完成的旧批次，应只保留当前未完成点位。
- 后续每一轮反编译修复都必须先复扫，再把**新确认的未完成点位**补录进附录B。
