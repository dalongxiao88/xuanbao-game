# 当前项目反编译残留审查报告（2026-03-12）

**审查日期：** 2026-03-12  
**审查范围：** `GameClient/src/`、`Gameserver/src/main/java/`  
**不计入范围：** `GameClient/jre/` 内置运行时、`Gameserver/src/main/webapp/` 第三方静态资源、数据库脚本、历史 Markdown 文档、二进制资源文件  
**审查目的：** 基于当前代码现状判断项目中还剩多少反编译代码残留，并区分“硬残留”和“轻残留”。

---

## 1. 审查结论

- 当前源码目录中的**硬反编译残留为 `0` 个文件**。
- 当前源码目录中的**轻反编译残留为 `0` 个文件**。
- 当前源码目录中已未发现 `varN` 级别的轻度残留，也未发现新的 `JD-Core` 头、`typeN(...)`、`getSI2`、`Splice`、`getPetSkillswl`、`p0/p1` 这类硬特征。
- 轻残留总出现次数约为 `0` 处。

---

## 2. 统计口径

### 2.1 硬残留

满足任一条件即计入：

- 源码中仍带 `JD-Core Version`、`FernFlower`、`Source code recreated from a .class file` 等反编译头/尾注。
- 源码中仍保留历史异常命名：
  - `ChangePart`
  - `getpart`
  - `getpartAll`
  - `getSI2`
  - `Splice`
  - `getPetSkillswl`
  - `ChangeTalent`
- 服务端 `mapper/service/thread` 链中仍保留 `p0/p1` 这类占位参数名。

### 2.2 轻残留

不再带硬特征，但仍残留以下轻度痕迹：

- `catch (Exception var3)` 这类异常变量占位命名。
- 局部变量 `var2 / var3 / var4` 这类可读性较差的反编译式命名。

### 2.3 不视为反编译残留的内容

- 业务拼音命名，例如 `lingbao`、`lianghao`、`qizhi`、`xiuwei`。
- 公式变量 `p1 / p2 / p3` 这类普通局部计算变量。
- 历史审查文档中提到的旧名称或旧统计数字。

---

## 3. 当前统计

| 维度 | 服务端 | 客户端 | 合计 |
|---|---:|---:|---:|
| Java 文件总数 | 1439 | 1858 | 3297 |
| 硬残留文件 | 0 | 0 | 0 |
| 轻残留文件 | 0 | 0 | 0 |
| 轻残留出现次数 | 0 | 0 | 0 |

---

## 4. 当前判断

- 项目中**已没有需要按“硬反编译残留”处理的源码文件**。
- 项目中**也已没有 `varN` 级别的轻反编译残留源码文件**。
- 从源码口径看，当前反编译残留清理阶段已经完成，后续应转入编译、打包与功能回归验证阶段。

---

## 5. 轻残留热点文件 Top 25

当前源码目录中已无轻残留文件，本节清空。

无。

---

## 6. 轻残留模块分布

按源码一级/二级目录聚合后的文件数：

| 文件数 | 目录 |
|---:|---|
| 0 | 无 |

---

## 7. 服务端轻残留清单（0）

- 当前服务端源码目录中已没有 `varN` 级别的轻残留文件。

---

## 8. 客户端轻残留清单（0）

- 当前客户端源码目录中已没有 `varN` 级别的轻残留文件。

---

## 9. 审查建议

1. 当前源码范围内的反编译残留已完成清理。
2. 后续主线应从“反编译收口”切换到“验证与回归测试”。
3. 服务端优先验证项：
   - `compile`
   - `package`
   - 后台管理接口
4. 客户端优先验证项：
   - 高频界面打开链路
   - 面板交互
   - 网络协议收发
